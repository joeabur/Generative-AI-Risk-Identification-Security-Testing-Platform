# Third-party components

Every tool this platform integrates or adapts, with its licence and how it is
used. Exact pinned versions for the Python dependencies are in
`backend/pyproject.toml` and the lock file; the SBOM (CycloneDX) generated by
`.github/workflows/sbom.yml` is the authoritative machine-readable list.

## Security tools this platform runs

| Tool | Licence | Used for | Notes |
|---|---|---|---|
| **Semgrep** | LGPL-2.1 (CLI) | SAST | Run with the bundled rule set under `app/core/appsec/sast/rules` |
| **Bandit** | Apache-2.0 | SAST (Python) | |
| **pip-audit** | Apache-2.0 | SCA | Advisory lookup is **opt-in**: it sends the dependency list to a third party |
| **detect-secrets** | Apache-2.0 | Secrets, CI baseline | |
| **Gitleaks** | MIT | Secrets in git history | Run with `--redact`; the report file is deleted in a `finally` |
| **Checkov** | Apache-2.0 | IaC | |
| **Trivy** | Apache-2.0 | Container packages | Run offline (`--skip-db-update --offline-scan`) |
| **CodeQL** | GitHub terms | CI analysis of this repository | Our own CI only, not run against targets |

If a tool is not installed, its engine reports `AEGIS-APPSEC-000 — not tested`
naming the tool, rather than returning nothing.

## Runtime dependencies

| Component | Licence | Role |
|---|---|---|
| FastAPI | MIT | API framework |
| Starlette | BSD-3-Clause | ASGI toolkit |
| Uvicorn | BSD-3-Clause | ASGI server |
| Pydantic / pydantic-settings | MIT | Validation and configuration |
| SQLAlchemy | MIT | ORM (2.x async) |
| asyncpg | Apache-2.0 | PostgreSQL driver |
| Alembic | MIT | Migrations |
| Celery | BSD-3-Clause | Task queue |
| Redis (server, client) | RSALv2/SSPLv1 (server), MIT (client) | Broker and kill switch. The server is a deployment dependency, not vendored |
| PostgreSQL | PostgreSQL Licence | Storage. A deployment dependency, not vendored |
| httpx | BSD-3-Clause | HTTP client — **only** inside `GatedTransport` |
| structlog | Apache-2.0 / MIT | Structured logging |
| argon2-cffi | MIT | Password hashing |
| PyJWT | MIT | Session tokens |
| WeasyPrint | BSD-3-Clause | PDF rendering |
| Jinja2 | BSD-3-Clause | HTML report templates |
| Next.js | MIT | Frontend scaffold |
| React | MIT | Frontend |
| Tailwind CSS | MIT | Frontend styling |

## Development and CI

| Component | Licence | Role |
|---|---|---|
| pytest, pytest-asyncio | MIT | Test runner |
| Hypothesis | MPL-2.0 | Property tests (redaction) |
| respx | BSD-3-Clause | Transport mocking |
| ruff | MIT | Lint and format |
| mypy | MIT | Type checking |
| import-linter | BSD-2-Clause | Enforces the assistant layer's dependency direction |
| Vitest | MIT | Frontend tests |

## Vendored data

| Item | Source | Notes |
|---|---|---|
| SARIF 2.1.0 schema | OASIS | Vendored at `backend/tests/schemas/` so validation does not need the network |
| End-of-life table | Vendor schedules | `app/core/appsec/supplychain/eol.py`, with `AS_OF` stated in every finding |
| Licence classifier map | SPDX + Trove classifiers | `app/core/appsec/supplychain/licenses.py` |
| Framework editions | See `docs/frameworks.md` | Pinned with retrieval dates |

## Licence compatibility

This project is Apache-2.0 (`docs/decisions/0004-licence.md`). The runtime
dependencies above are permissive and compatible.

Semgrep's CLI is LGPL-2.1 and Hypothesis is MPL-2.0 — both weak copyleft, both
used as **separate processes or unmodified libraries**, which is the usage those
licences are written for. Neither is modified or statically linked. If you
redistribute a modified Semgrep or Hypothesis, their terms apply to that
modification, not to this project.

Redis's server licence (RSALv2/SSPLv1) applies to running Redis, not to software
that connects to it. This project ships no Redis binary.

The platform's own licence engine (`docs/supply-chain.md`) can be pointed at
this repository, and this table is a reasonable thing to check it against.
