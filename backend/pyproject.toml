[project]
name = "aegis-ai-security-backend"
version = "0.1.0"
description = "Aegis AI Security — backend API, orchestrator, and security testing engine"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.30",
    "alembic>=1.13",
    "argon2-cffi>=23.1",
    "pyjwt>=2.9",
    "celery>=5.4",
    "redis>=5.1",
    "structlog>=24.4",
    "httpx>=0.27",
    "python-multipart>=0.0.12",
    "email-validator>=2.2",
    "idna>=3.10",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "respx>=0.21",
    "hypothesis>=6.115",
    "ruff>=0.7",
    "mypy>=1.13",
    "coverage>=7.6",
    "pytest-cov>=5.0",
    "types-PyYAML>=6.0",
    "jsonschema>=4.23",
    "types-jsonschema>=4.23",
]
# PDF reporting (§14). Optional because WeasyPrint needs system Pango and
# Cairo libraries; every other report format works without it, and the PDF
# endpoint says what is missing rather than failing obscurely.
pdf = [
    "weasyprint>=62",
]
# The AppSec scanners. Optional at runtime by design (§15): an adapter whose
# tool is absent reports that it did not run rather than crashing or, worse,
# returning nothing and looking clean. They are in `dev` as well so CI can
# actually exercise the engines against the lab fixtures.
appsec = [
    "semgrep>=1.90",
    "bandit>=1.7",
    "pip-audit>=2.7",
    "checkov>=3.2",
    "detect-secrets>=1.5",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ASYNC"]

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]
exclude = ["alembic/"]

# Celery ships no type stubs / py.typed marker, so its decorators are
# untypeable from the caller's side; scoped to app.workers rather than
# disabled globally.
[[tool.mypy.overrides]]
module = "app.workers.*"
disallow_untyped_decorators = false

[[tool.mypy.overrides]]
module = "celery.*"
ignore_missing_imports = true

# WeasyPrint ships no py.typed marker. Optional dependency, imported in one
# function, and its return value is narrowed at the call site.
[[tool.mypy.overrides]]
module = "weasyprint.*"
ignore_missing_imports = true

[tool.coverage.run]
# SQLAlchemy's asyncpg dialect bridges async/sync via greenlet; without this,
# coverage.py silently under-reports every line that runs inside one of
# those greenlets (most DB-touching code), since sys.settrace does not
# propagate to a new greenlet by default.
concurrency = ["greenlet", "thread"]
source = ["app"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "session"
asyncio_default_test_loop_scope = "session"
testpaths = ["tests"]
