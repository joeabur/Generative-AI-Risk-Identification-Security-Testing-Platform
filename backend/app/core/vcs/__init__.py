"""Posting findings back to a pull request.

See `contract.py` for what this layer may and may not do, and
`docs/pull-requests.md` for why "review comments and a check run" is the whole
of it — no commits, no merges, no autofix.
"""
